# Deployment Checklist (GitHub Pages)

1. git add .
2. git commit -m "Describe change"
3. git push origin main
4. npm run deploy   ← REQUIRED(for gh-pages)
5. Wait 1–2 minutes
6. refresh website

